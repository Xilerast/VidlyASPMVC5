@using Vidly.Models
@*@model Vidly.Models.Movie*@
@model Vidly.ViewModels.RandomMovieViewModel
@{
    ViewBag.Title = "Random";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{ 
    var className = (Model.Customers.Count > 5) ? "popular" : null;
}

@*<h2>@Model.Name</h2> NOT SO BAD WITHOUT VIEWMODELS. MAKE SURE TO @model Vidly.Models.Movie for this *@
<h2 class=@className>@Model.Movie.Name</h2>

@if (Model.Customers.Count == 0)
{
    <text>There's no customers that rented this movie before.</text>
}
else
{
<ul>
    @foreach (var customer in Model.Customers)
    {
        <li>
            @customer.Name
        </li>
    }
</ul>
}
@* BAD <h2>@( ((Movie) ViewData["Movie"]).Name )</h2>*@
@* BAD <h2>ViewBag.RandomMovie</h2>*@
